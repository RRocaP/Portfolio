{
  "name": "portfolio-visual-pipeline",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "engines": { "bun": ">=1.0.0" },
  "scripts": {
    "start": "bun run orchestrate.ts",
    "overnight": "bun run orchestrate.ts --overnight",
    "ci": "bun run orchestrate.ts --ci",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.63",
    "@vercel/blob": "^2.3.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.0"
  }
}
