---
import Timeline from './Timeline.tsx';

export interface Props {
  lang: 'en' | 'es' | 'ca';
  strings: any;
}

const { lang, strings } = Astro.props;
---

<Timeline lang={lang} strings={strings} client:visible />
