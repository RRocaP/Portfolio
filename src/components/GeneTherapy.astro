---
// Wrapper component for the Gene Therapy Visualization
---

<div id="gene-therapy-container" class="visualization-wrapper">
  <div id="gene-therapy-root"></div>
</div>

<script>
  import GeneTherapyVisualization from './GeneTherapyVisualization.tsx';
  import React from 'react';
  import ReactDOM from 'react-dom/client';

  // Wait for DOM to be ready
  document.addEventListener('DOMContentLoaded', () => {
    const container = document.getElementById('gene-therapy-root');
    if (container) {
      const root = ReactDOM.createRoot(container);
      root.render(React.createElement(GeneTherapyVisualization));
    }
  });
</script>

<style>
  .visualization-wrapper {
    margin: 3rem 0;
    padding: 2rem;
    background: var(--color-bg-primary);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
  }

  @media (max-width: 768px) {
    .visualization-wrapper {
      padding: 1rem;
      margin: 2rem 0;
    }
  }
</style>