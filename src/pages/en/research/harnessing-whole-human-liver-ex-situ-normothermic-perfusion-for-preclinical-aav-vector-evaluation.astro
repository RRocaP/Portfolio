---
import ResearchLayout from '../../../layouts/ResearchLayout.astro';
import { publications } from '../../../data/publications.js';
const project = publications.find(p => p.title === "Harnessing whole human liver ex situ normothermic perfusion for preclinical AAV vector evaluation");
---
<ResearchLayout
  title={project.title}
  description={project.description}
  lang="en"
  project={project}
/>
